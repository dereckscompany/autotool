% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_handler.R
\name{make_handler}
\alias{make_handler}
\title{Build a tool-call handler from a function and pinned defaults}
\usage{
make_handler(fn, defaults = list())
}
\arguments{
\item{fn}{The function to invoke.}

\item{defaults}{Named list of pinned arguments (typically the same
list passed to \code{\link[=generate_tool]{generate_tool()}}).}
}
\value{
A function of a single argument (\code{model_args}) that returns
whatever \code{fn} returns.
}
\description{
Companion to \code{\link[=generate_tool]{generate_tool()}}. Returns a 1-argument closure that
merges the supplied \code{defaults} under whatever argument list the LLM
produced at tool-call time, then invokes \code{fn}. Drop the closure into
your handler registry, call it with the model's parsed arguments.
}
\details{
The merge gives the model's arguments precedence: anything the model
supplies for a key in \code{defaults} overrides the pinned value. That is
usually correct — hidden arguments have defaults the model cannot see,
so it has no way to supply them — but if you want strict pinning,
validate before calling.

\code{fn} and \code{defaults} are forced at factory-call time, so reassigning
the surrounding bindings later does not change the handler's
behaviour.
}
\examples{
add <- function(x, y, scale = 1) (x + y) * scale
handler <- make_handler(add, defaults = list(scale = 10))
handler(list(x = 2, y = 3))
# 50

}
\seealso{
\code{\link[=generate_tool]{generate_tool()}}.
}
