% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_with_defaults.R
\name{call_with_defaults}
\alias{call_with_defaults}
\title{Dispatch a model tool call after merging hidden defaults}
\usage{
call_with_defaults(fn, model_args, defaults = list())
}
\arguments{
\item{fn}{The function to invoke.}

\item{model_args}{A (possibly empty) named list of arguments the
model produced.}

\item{defaults}{Named list of pinned defaults (typically the same
one passed to \code{\link[=generate_tool]{generate_tool()}}).}
}
\value{
Whatever \code{fn} returns.
}
\description{
Companion to \code{\link[=generate_tool]{generate_tool()}}. When the model emits a tool call,
you receive an argument list it produced from the visible schema.
This function merges those args with the \code{defaults} you pinned at
definition time (credentials, base URLs, etc.) and invokes the
underlying function.
}
\details{
The merge gives the model's args precedence: anything the model
supplies for a key in \code{defaults} will override the pinned value.
That is usually correct — hidden args have defaults the model never
sees, so it cannot supply them — but if you want strict pinning,
validate before calling.
}
\examples{
add <- function(x, y, scale = 1) (x + y) * scale
call_with_defaults(add, list(x = 2, y = 3), defaults = list(scale = 10))
# 50

}
\seealso{
\code{\link[=generate_tool]{generate_tool()}}.
}
